(function(){async function save(owner,itemId,file){if(!file)return null;if(!['image/png','image/jpeg','image/webp'].includes(file.type)||file.size>5*1024*1024)throw Error('Imagem inválida ou acima de 5 MB');const img=await createImageBitmap(file),c=document.createElement('canvas'),max=800,scale=Math.min(1,max/Math.max(img.width,img.height));c.width=Math.round(img.width*scale);c.height=Math.round(img.height*scale);c.getContext('2d').drawImage(img,0,0,c.width,c.height);const blob=await new Promise(r=>c.toBlob(r,'image/webp',.8)),id=`sv-item-${owner}-${itemId}`;await SVImageStorage.put(id,blob);return id}window.SVItemImage={save}})();
