(function(){function download(p){const a=document.createElement('a'),blob=new Blob([JSON.stringify(p,null,2)],{type:'application/json'});a.href=URL.createObjectURL(blob);a.download=`dossie-${p.id}-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href)}function validate(x){return x&&typeof x.id==='string'&&x.attributes&&x.stats&&Array.isArray(x.inventory)}function importFile(file,current,done){const reader=new FileReader();reader.onload=()=>{try{const data=JSON.parse(reader.result);if(!validate(data)||data.id!==current.id)throw Error('Arquivo incompatível com este agente');localStorage.setItem(SVSheet.key(current.id,'backup'),JSON.stringify(current));done(data)}catch(e){alert(e.message)}};reader.readAsText(file)}window.SVBackup={download,validate,importFile}})();
