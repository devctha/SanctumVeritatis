(function(){async function apply(){for(const card of document.querySelectorAll('[data-master-agent]')){if(card.querySelector('.master-thumb'))continue;const id=card.dataset.masterAgent,meta=SVProfile.portraitMeta(id),blob=meta.thumbnailId&&await SVImageStorage.get(meta.thumbnailId);if(!blob)continue;const img=document.createElement('img');img.className='master-thumb';img.src=URL.createObjectURL(blob);img.alt=`Retrato de ${id}`;card.prepend(img)}}new MutationObserver(apply).observe(document.documentElement,{childList:true,subtree:true});apply()})();
