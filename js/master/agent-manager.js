(function(){function profile(user){return SVSheet.load(user)}function save(p){return SVSheet.save(p)}function configuration(form){const id=form.username.toLowerCase().replace(/[^a-z0-9]+/g,'-');return{id,username:form.username,displayName:form.displayName,role:form.role,accessLevel:Number(form.accessLevel),route:form.route||`agentes/${id}/index.html`,profileStorageKey:`sv_agent_${id}`,active:true}}window.SVAgentManager={profile,save,configuration}})();
