import{authenticate,session}from'./auth.js';
const form=document.querySelector('#login-form'),status=document.querySelector('#auth-status'),button=form.querySelector('button[type="submit"]');
if(session())status.textContent='Sessão anterior reconhecida. Uma nova autenticação substituirá o enlace atual.';
form.addEventListener('submit',async event=>{event.preventDefault();button.disabled=true;form.dataset.state='checking';status.textContent='Validando assinatura…';const result=await authenticate(form.agent.value,form.key.value,form.remember.checked);await new Promise(resolve=>setTimeout(resolve,650));if(result.ok){form.dataset.state='authorized';status.textContent=`Acesso autorizado. Bem-vindo, ${result.session.codename}.`;const next=new URLSearchParams(location.search).get('next');setTimeout(()=>location.href=next||'sistema/dashboard.html',700)}else{form.dataset.state=result.locked?'locked':'error';status.textContent=result.locked?'Terminal bloqueado localmente após cinco falhas. Limpe o estado do navegador para reiniciar.':`Credencial recusada. ${result.remaining} tentativa(s) restante(s).`;button.disabled=false;form.key.select()}});
